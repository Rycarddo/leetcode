class Solution {
    public String longestCommonPrefix(String[] strs) {
        int n = strs.length;

        int size = 0;

        // taking the size of the smallest string
        for (int i = 0; i < n; i++) {
            String small = "";
            
            if (strs[i].length() < small.length()) {
                small = strs[i];
                size = small.length();
                
            };
        };

        for (int i = 0; i < n; i++) {
            for (int j = 0; j < )
        }

    }
}